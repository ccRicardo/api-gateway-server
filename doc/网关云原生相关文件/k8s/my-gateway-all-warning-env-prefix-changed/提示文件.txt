待网关应用开发完毕后，将其涉及的所有k8s资源配置文件都放到该目录下。
注意：
* 网关配置的环境变量前缀发生了变化，由GATEWAY_变成了gateway_
* 需要将最终的jar包打成docker镜像，然后上传到k8s集群的每个节点仓库中
* 将rule的获取方式改为了从serviceInvoker属性中获取，客户端的方法调用注解需加上ruleId，nacos配置方面也需要做相应修改。
* server和client的common和注册中心模块需要同步！！！
* 配置中心的逻辑大改，nacos配置需要全面修改：添加元数据文件；一个文件对应一个规则对象。